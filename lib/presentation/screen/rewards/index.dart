import 'package:flutter/material.dart';

class RewardsRoute extends StatelessWidget {
  const RewardsRoute({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rewards Route'),
      ),
    );
  }
}